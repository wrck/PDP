# PDP API ClusterIP 服务
apiVersion: v1
kind: Service
metadata:
  name: pdp-api
  namespace: pdp
  labels:
    app: pdp
    component: api
spec:
  type: ClusterIP
  selector:
    app: pdp
    component: api
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
