# PDP 企业级项目交付管理平台实施计划

**分支**：`002-pdp-product` | **日期**：2026-07-16 | **规格**：[spec.md](spec.md)

**输入**：L0 产品规格 `specs/002-pdp-product/spec.md`

## 摘要

PDP 首期交付“平台基础能力 + 标准实施领域包”的可上线闭环，覆盖工作空间治理、领域包深度定制、项目模板与生命周期、计划执行、交付件、统一审批、风险问题变更、多视图、权限审计、高可用及现有 MySQL 历史数据迁移。技术上采用可水平扩展的模块化单体：以稳定核心模型承载符合 ISO 21500 系列的统一项目管理语义，以元数据运行时承载字段、对象、页面、状态、规则和动作定制，以 Flowable Process Engine 建设平台级 BPMN 2.0.2 工作流基础能力，以受治理扩展接口承载专业算法或设备能力；模块间通过显式 API 和事务事件协作，为后续按容量或组织边界拆分服务保留路径。审批、项目交付和领域包通过统一工作流端口复用定义、实例、任务、定时、重试、补偿和诊断能力；Flowable 只负责编排协调，领域状态机与审批聚合仍是业务事实源。P1 只认证并实现 MySQL 8.4 LTS，但领域层、应用层、领域包和外部契约保持数据库无关；PostgreSQL 适配器、认证和跨数据库切换进入 P2。MySQL 历史数据先进入隔离暂存区，经版本化映射、全量加 CDC、业务核对和冻结切换后进入 PDP MySQL 主库。

本计划只承诺 P1。P2/P3 仅保留兼容边界和演进接口，不在首期实现资源、成本、移动离线、六类行业包全集、AI 等完整能力。

## 技术背景

**语言与版本**：后端 Java 21 LTS；前端 TypeScript 5.x、Vue 3.5；P1 数据库基线 MySQL 8.4 LTS；历史数据迁移支持 MySQL 5.7/8.x 源；契约 YAML/JSON

**主要依赖**：Spring Boot 4.1、Spring Modulith 2.1、`spring-modulith-events-jdbc`、Spring Security、Flowable 8.0.x `flowable-spring-boot-starter-process`、MyBatis-Plus 3.5.17 Spring Boot 4 Starter、dynamic-datasource 4.5.0 Spring Boot 4 Starter、MyBatis、HikariCP、Liquibase、Vue 3.5、Vite 8、Vue Router、Pinia、Ant Design Vue、TanStack Vue Query、JSON Schema

**存储**：P1 每个部署使用一个 MySQL 8.4 LTS 业务事实真源；dynamic-datasource 受控路由 `pdpPrimary`、可选 `pdpRead`、`migrationSource` 和 `migrationTarget`，Flowable 使用独立 `workflowEngine` schema/账号/数据源；每个数据源使用独立 HikariCP 池；现有 MySQL 作为迁移期只读源；S3 兼容对象存储保存附件与交付件文件；Redis 仅用于缓存、限流和短期协调，不作为业务真源

**测试**：JUnit 5、Spring Modulith 模块测试、Flowable BPMN 部署/迁移/异步执行器测试、MySQL 8.4 Testcontainers 数据库矩阵、动态数据源路由与 HikariCP 故障测试、Redis 与对象存储容器、ArchUnit、Vitest、Vue Test Utils、Testing Library for Vue、Playwright、迁移核对、契约校验与 k6 性能测试

**目标平台**：Linux 容器；开发环境支持 Docker Compose；生产环境支持 Kubernetes 兼容平台、托管 MySQL 8.4 以及 S3 兼容对象存储

**项目类型**：企业级 Web 应用，单仓库、前后端分离、后端模块化单体

**性能目标**：1000 名并发活跃用户；95% 常用页面在 2 秒内呈现可用结果；百万级记录下 95% 常用搜索和报表在 3 秒内返回或先呈现渐进结果

**约束**：核心业务可用性不低于 99.95%，其他在线能力不低于 99.9%，统一按 FR-165 统计；RTO 不超过 30 分钟、RPO 不超过 5 分钟；权限撤销满足新请求立即生效、缓存 5 秒、搜索/报表 30 秒、活动会话 1 分钟等时限；核心事务不得依赖搜索、报表、AI 或非关键外部系统；MySQL 历史迁移必须可追溯、可核对、可重跑，并具有明确切换与回退边界；业务与领域代码不得依赖数据库专有能力

**规模与范围**：P1 覆盖 14 个业务与运行模块（含 `workflow`）、1 个公共持久化基础模块、1 个 MySQL 适配器、1 个标准实施领域包、1 条真实或等价项目端到端试点、1 套 MySQL 历史迁移与切换方案；预留多工作空间、百万级对象、三层领域包继承、后续数据库适配和外部集成扩展

## 宪章检查

项目宪章版本为 2.2.0。本计划在研究前和设计完成后按以下不可协商门禁复核：

| 宪章门禁 | 规划前检查 | 设计后复核 |
|---|---|---|
| I. 产品规格与自顶向下追踪 | 通过 | 通过，计划、契约和任务均追溯 P1 用户故事、FR 与 SC |
| II. 业务闭环与纵向增量交付 | 通过 | 通过，P1 以标准实施项目从创建到归档的完整闭环组织，P2/P3 留在 backlog |
| III. 统一领域语言与确定性状态 | 通过 | 通过，术语以 ISO 21500 系列、PMI Lexicon 和 BPMN 2.0.2 为基线，核心状态机与流程编排事实边界已定义 |
| IV. 稳定内核、受治理扩展与简单演进 | 通过 | 通过，采用稳定内核、三层领域包和模块化单体；新增复杂度由容量或边界证据触发并记录 ADR |
| V. 契约优先、兼容演进与数据库独立 | 通过 | 通过，P1 仅实现 MySQL，领域、应用和契约保持数据库无关，PostgreSQL 下移 P2 |
| VI. 最小授权、隔离、审计与数据治理 | 通过 | 通过，API、缓存、投影、附件、导出、会话和迁移均统一授权并规划威胁验证 |
| VII. 可执行质量与证据化验收 | 通过 | 通过，每个故事具有独立失败测试、持久化四件套、MySQL 矩阵和证据任务 |
| VIII. 可观测、可恢复与安全迁移 | 通过 | 通过，定义 SLI/SLO、容量、关联标识、降级、运行手册、迁移检查点和恢复演练 |
| IX. 结果导向与可解释的用户体验 | 通过 | 通过，跨视图统一状态和权限，高风险操作具有预览/冲突反馈，并规划代表性用户研究 |
| 当前 P1 与工程门禁 | 通过 | 通过，Flowable 受限编排、严格动态数据源、独立 HikariCP/会话工厂、MySQL 单写主库和历史迁移均有任务 |
| DoR 与 DoD | 通过 | 通过，无未决标记；最终阶段覆盖安全、性能、容量、恢复、迁移、可用性和签字证据 |

不存在未解释的门禁失败。

## 业务闭环与领域设计

- `spec.md` 的 P1 业务闭环矩阵是故事拆分和独立验收的输入；每个故事必须保持角色、触发、状态变化、
  输出、异常、补偿和证据的完整链路，不得仅交付页面、接口或数据库表。
- `data-model.md` 维护工作空间、领域包、项目、阶段、任务、里程碑、基线、交付件、审批、风险、问题、
  变更、后台作业和迁移的确定性状态机。应用服务执行状态迁移，Flowable 与审批流程仅协调，不保存
  唯一业务事实。
- `spec.md` 的术语目录以 ISO 21506/21502/21511、PMI Lexicon 和 OMG BPMN 2.0.2 为来源；代码、
  接口、事件和数据库使用稳定英文键，界面别名不得改变规范含义。
- 状态迁移统一校验工作空间、对象版本、前置条件、权限、并发和不变量；失败返回稳定原因、阻断项、
  当前版本和下一步建议，并关联审计与链路标识。
- 领域包可以增加领域子状态和动作，但必须映射平台顶层生命周期，不得改变平台核心身份、权限、
  审计、版本、保留或单写主权。
- 高风险操作统一接入影响预览、明确确认、审计和撤销/回退/补偿框架，覆盖领域发布迁移、项目回退、
  基线替换、人工进度、交付件发布、审批终态、导出处置和历史迁移上线切换。

## 架构与模块分解

### 运行时边界

1. **Web 前端**：提供平台管理、领域设计、项目工作区和统一待办入口。
2. **PDP 应用**：承载同步命令、查询、权限决策和核心事务；保持无状态以支持多副本。
3. **Flowable 流程引擎**：嵌入 PDP 应用，仅启用 Process Engine，通过 `workflow` 模块执行版本化
   BPMN 定义；不直接暴露 REST API，不保存权威业务结论。
4. **后台任务执行器**：与 PDP 应用共享模块和数据库，但以独立运行配置处理 Flowable 异步作业、
   导入导出、索引、通知、迁移和集成重试。
5. **关系数据库适配层**：P1 连接 MySQL 8.4，保存核心数据、领域元数据、运行实例、审计和事务事件日志；每个部署仅允许一个写入主库，并通过公共端口保留后续数据库适配边界。
6. **对象存储**：保存文件内容；数据库保存文件元数据、版本、哈希和授权关系。
7. **Redis**：缓存可重建数据、限流和短租约；失效时核心事务降级为直接访问数据库。
8. **迁移暂存区**：保存 MySQL 原始快照、增量位点、标准化中间数据、问题隔离和核对结果；只能由迁移服务访问，不向在线业务开放。

### 业务模块

| 模块 | P1 职责 | 主要追溯 |
|---|---|---|
| `identity` | 企业身份映射、会话、用户停用、授权版本和撤销时限 | FR-063～FR-069、FR-164 |
| `workspace` | 工作空间、组织、成员、角色、跨空间授权 | FR-003～FR-006、FR-121～FR-124 |
| `domainconfig` | 领域包、对象/字段/页面/状态/规则、版本、发布和迁移 | FR-007～FR-020、FR-130～FR-134 |
| `template` | 项目模板及批准版本 | FR-021～FR-022 |
| `project` | 项目、阶段、父子项目、顶层生命周期 | FR-023～FR-028、FR-117～FR-120 |
| `planning` | 任务、检查项、里程碑、依赖、基线、进度 | FR-029～FR-037、FR-125～FR-129 |
| `deliverable` | 交付件、版本、发布、签核和归档清单 | FR-038～FR-042 |
| `approval` | 统一审批定义、实例、节点、委托与回写 | FR-043～FR-046 |
| `workflow` | 平台级 Flowable 基础能力：BPMN 定义/部署、实例/人工任务、定时/消息、业务关联、异步执行、重试/补偿、异常诊断和流程迁移 | FR-009、FR-017、FR-043～FR-046、FR-174 |
| `governance` | 风险、问题、变更、审计、数据保留 | FR-047～FR-052、FR-069～FR-071 |
| `experience` | 列表、看板、日历、时间线、保存视图、P1 基础搜索与站内通知 | FR-053～FR-060 |
| `standarddelivery` | 标准实施领域包及从创建到归档的闭环 | FR-094、FR-135～FR-138 |
| `integration` | P1 仅提供 OIDC、对象存储、内部事务事件、幂等重试和健康度基础；外部命令与订阅属于 P2 | FR-063、FR-066～FR-069 的技术支撑 |
| `operations` | 可用性、性能、后台作业、备份恢复、降级、SLI 和运行观测 | FR-102～FR-111、FR-165 |
| `datamigration` | MySQL 盘点、映射、全量/增量迁移、隔离、核对、切换与旧系统退役 | FR-139～FR-150 |
| `persistence` | P1 MySQL 能力检测、公共持久化契约、方言与变更集，以及后续数据库适配边界 | 宪章原则 V、FR-139～FR-150、FR-171～FR-172 |

模块只能依赖其他模块公开的应用服务、只读查询接口或领域事件；禁止跨模块直接访问内部表和实现类。CI 使用 Spring Modulith/ArchUnit 校验依赖方向和循环引用。

### 平台工作流基础能力与 Flowable 边界

- 采用 Flowable 8.0.x，并由项目 BOM 锁定经过 Spring Boot 4.1、Java 21 和 MySQL 8.4 验证的
  补丁版本；仅引入 `flowable-spring-boot-starter-process`。
- 不引入 Flowable 全引擎 starter、REST starter、IDM、CMMN、DMN 或 JPA/Hibernate 集成。PDP
  身份、授权、审批待办和业务查询继续使用自身模型。
- `workflow` 是平台公共基础模块，向审批、项目、交付件、治理和领域包运行时提供
  `WorkflowDefinitionPort`、`WorkflowRuntimePort`、`WorkflowTaskPort` 和
  `WorkflowAdministrationPort`；业务模块不得依赖 Flowable API、实体、表名或异常类型。
- BPMN 定义使用稳定 process key、语义化业务版本、内容哈希和领域包版本关联；已启动实例默认固定
  启动时定义版本，迁移必须预览、分批、可暂停并保留证据。
- Flowable 表与 PDP 业务表使用独立 schema/表前缀、数据库账号、HikariCP 池和事务管理器；不使用
  XA。业务事实先在本地事务提交并登记 outbox，再由幂等编排消费者启动或推进流程。
- 生产关闭 Flowable 自动建表和自动升级；引擎 DDL/升级脚本版本化管理，并在 MySQL 8.4 上执行
  空库初始化、跨版本升级、回退边界和备份恢复测试。
- Flowable 用户任务只保存 `approval_instance_id`、`business_object_ref` 等最小关联；平台待办由
  统一工作流任务端口投影，候选人和办理权限由 PDP 实时计算与复核，不能依赖引擎内置身份数据作授权。
- 异步执行器使用独立线程池、队列、连接预算和指标；重试耗尽形成可检索 incident/dead-letter，
  支持安全重放与人工补偿，不得重复形成审批结论或业务状态变化。

### P1 MySQL 持久化与数据库独立边界

- 领域层和应用层只依赖仓储、查询、锁、分页、搜索投影和事务端口，不引用数据库驱动、方言类或专有 SQL。
- `public-persistence` 维护逻辑类型、Mapper 端口、公共 SQL 片段、结果映射、查询契约和数据库能力模型；P1 的 `persistence-mysql` 只实现 MySQL 差异化 DDL、索引、查询和运维检查，`persistence-postgresql` 不进入 P1 制品。
- P1 应用制品通过明确的 MySQL 连接配置启动，并验证产品必须为 MySQL、版本为认证的 8.4、schema、字符集、排序规则、时区、事务引擎和必要权限。
- MyBatis-Plus 只在基础设施层提供单表 CRUD、分页、乐观锁和安全拦截能力；业务模块不得直接暴露或依赖 `BaseMapper`、`IService`、`QueryWrapper` 等框架类型。
- 公共查询优先使用参数化的标准 SQL；复杂查询使用显式 Mapper XML。数据库专用语句通过 MyBatis `databaseId` 或独立适配器 Mapper 选择，禁止在业务服务中按数据库类型拼接 SQL。
- dynamic-datasource 设置 `primary=pdpPrimary`、`strict=true`；`@DS` 只允许出现在持久化和迁移基础设施实现，事务开始后禁止切换路由。
- `pdpRead` 仅用于明确允许最终一致的查询；审批、权限、写命令、读后写和事件发布始终访问 `pdpPrimary`。迁移源/目标使用独立 Mapper、事务管理器和连接池。
- 每个数据源使用独立 HikariCP 池；在线池总连接预算不超过数据库可用连接的 70%，池大小、超时、keepalive 和 maxLifetime 必须经容量压测冻结。
- MySQL JSON/生成列/FULLTEXT 只能作为适配器内优化；权限、状态、审计和业务正确性必须由公共模型保证，关闭优化后仍需通过正确性契约。
- Liquibase 变更集分为 `common` 和 `mysql`；MySQL 专用变更使用 `dbms=mysql` 条件并接受 MySQL 空库、升级和失败恢复测试。
- 每个 P1 持久化故事必须同时交付业务模块仓储端口、公共 Liquibase 变更集、公共 Mapper XML 或适配器、必要的 MySQL 专用索引以及 MySQL 仓储契约测试；不能只创建领域模型和应用服务。
- 生产环境不支持通过修改连接串切换到空库、旧快照或未核对目标。P1 的切换仅指历史 MySQL 旧系统到 PDP MySQL 的受控上线切换；PDP 认证数据库之间的跨库切换属于 P2。

### MySQL 历史数据迁移边界

- 异构迁移工具只负责可靠提取和传输，不直接决定 PDP 业务语义。
- 原始 MySQL 数据先进入隔离的迁移暂存区，再由版本化映射转换为 PDP 命令或受控批量装载格式。
- `migrationSource` 与 `migrationTarget` 分别使用独立 `SqlSessionFactory`、Mapper 扫描包、本地事务管理器、账号和 HikariCP 池；源事务只读，目标批次只在目标本地事务提交。
- 核心对象必须经过必填、唯一、关系、状态、权限和审计校验；无法映射的数据进入隔离区，不得静默丢弃。
- 全量迁移后通过 MySQL binlog 或等价变更源捕获增量；正式切换采用“冻结旧系统写入—应用最终增量—核对—启用 PDP—旧系统只读”的单向切换。
- 不采用长期双写。PDP 开始接收新写入后，回切旧系统必须先有经过验证的反向同步；否则采用前向修复。
- 详细持久化契约见 [persistence-design.md](persistence-design.md)，技术比较见 [technology-comparison.md](technology-comparison.md)，迁移执行方案见 [mysql-migration.md](mysql-migration.md)。

### 领域定制分层

```text
平台稳定内核
  └─ 平台标准包
      └─ 行业领域包
          └─ 工作空间客户包
```

- 稳定内核维护工作空间归属、统一标识、核心字段、权限、审计、版本和保留动作。
- 声明式元数据支持核心对象扩展与全新业务对象，并可定义字段、关系、页面、视图、状态、规则、动作、权限和集成映射。
- 专业算法、设备协议或高计算逻辑通过签名扩展包和受限服务接口接入，禁止访问内部表或提升当前用户权限。
- 运行实例绑定已发布领域包版本快照；升级通过预检、映射、分批迁移、报告和回滚完成。

### 安全、数据与运行设计

#### 实现前治理基线

- 在任何业务故事实现前完成模块化单体、MySQL 持久化与数据库独立边界、动态数据源、Flowable 工作流边界和迁移架构五项 ADR，并记录替代
  方案、质量属性、数据影响、验证方式和复审条件。
- 建立平台威胁模型与数据分类目录，覆盖身份、工作空间、字段、附件、签名、导出、迁移、数据库凭据、
  审计和后台作业；严重或高风险项必须关闭或形成有期限的正式例外。
- 审计采用只追加和摘要链/等价防篡改校验，敏感值只保存摘要或脱敏快照；凭据和密钥只保存外部密钥
  引用，并定义轮换、撤销和泄漏响应。
- 冻结 P1 契约覆盖矩阵、状态机测试矩阵、高风险操作目录、服务等级档案和证据目录后，执行首次
  `/speckit-analyze`；存在 CRITICAL/HIGH 时不得进入工程实现。

#### P1 服务等级与容量矩阵

| 能力类别 | SLI/SLO | 容量假设 | 主要失败模式与告警 | 责任与运行证据 |
|---|---|---|---|---|
| 核心交付命令与查询 | 月度 ≥ 99.95%，交互 P95 ≤ 2 秒 | 1000 活跃并发，按规格事务混合 | 应用、数据库、对象存储、权限或事件登记失败；5 分钟内通知值守 | `operations` 与业务模块共同负责，证据进入可用性报告和故事验收 |
| 配置、审计和运维管理 | 月度 ≥ 99.9%，P95 ≤ 3 秒 | 10 个工作空间、领域版本和审计规模按规格数据集 | 校验积压、审计写入失败、配置冲突；阻止发布并告警 | `domainconfig`、`governance`、`operations`，证据进入发布报告 |
| 搜索和基础统计 | 月度 ≥ 99.9%，P95 ≤ 3 秒或 1 秒渐进结果 | 百万任务、两百万活动/审计记录 | 投影滞后、重建、权限撤销延迟；显示截止时间并降级导航 | `experience` 与 `operations`，证据包含投影位点和撤权时限 |
| 后台作业 | 5 秒内接受并返回作业标识；检查点可恢复率 100% | 导入、导出、归档、统计和迁移批次受资源预算约束 | 队列积压、池耗尽、部分失败和死信；按阈值告警 | 作业所有模块与 `operations`，证据包含进度、失败明细和补偿 |
| 历史迁移与上线切换 | RTO ≤ 30 分钟、RPO ≤ 5 分钟，单写主权 100% | 两次全量彩排和 MySQL 旧系统到 PDP 的单向切换 | 增量落后、核对失败、双写、回退点越界；立即阻断切换 | `datamigration`、`persistence`、DBA 和业务签字人共同负责 |

服务等级档案以版本化目录维护，包含请求分类、交互时限、指标名称、阈值、告警、责任人、运行手册和
最近验证证据。容量测试采用规格定义的数据集和事务集，在生产等价拓扑预热 10 分钟并持续 30 分钟。

#### 用户体验与高风险操作保护

- P1/P2 通过代表性用户研究验证角色旅程、跨视图一致性、错误反馈和任务完成效率，不设置无障碍门禁。
- 全部无障碍能力归入 P3 最低优先级可选范围；只有建立独立子规格并获批后才规划实现和验收。
- 所有高风险界面必须展示对象版本、影响范围、阻断项、不可逆点和补偿路径；确认前不得执行写入。

## 实施阶段

### 阶段 A0：技术验证与迁移盘点

- 完成五项架构 ADR、标准术语目录、威胁模型、数据分类、状态机矩阵、高风险操作目录和 P1 服务等级档案。
- 完成 P1 HTTP、事件和领域包契约覆盖矩阵，创建迁移报告 Schema，并冻结消费者、版本和弃用规则。
- 使用 Vue 完成领域设计器、动态表单、看板、时间线、虚拟表格和字段级权限的技术原型，验证复杂组件选型与性能预算。
- 固化 Vue Composition API、SFC、Pinia、服务器状态、路由、组件分层和主题扩展规范，禁止在主应用引入第二套前端框架。
- 完成 MySQL 版本、结构、存储引擎、字符集、时区、对象规模、数据质量、存储过程、触发器和附件存储盘点。
- 产出源到目标映射目录、数据质量基线、全量耗时基线、增量方案和切换窗口评估。
- 使用生产等价数据完成 MySQL 8.4 schema 升级、持久化契约、核心查询、并发控制、性能和恢复验证，冻结 P1 认证版本及能力矩阵。
- 使用代表性审批、超时、退回、加签和补偿流程验证 Flowable 8.0.x Process Engine、Spring Boot
  4.1、独立数据源/连接池、MySQL DDL 与流程版本迁移，冻结 BPMN 建模约束和引擎升级边界。
- 使用脱敏生产等价数据验证“历史 MySQL → PDP MySQL 8.4”的迁移路径；即使同为 MySQL，也必须完成语义映射、标识转换、核对和切换门禁。
- 关闭规格质量检查表并执行实现前 `/speckit-analyze`；只有 CRITICAL/HIGH 为零才进入阶段 A。

### 阶段 A：工程与治理基线

- 建立后端模块骨架、前端壳、根 pnpm 测试工作区、Playwright/Spectral/AJV/k6 配置、统一错误模型、API 版本和可观测性。
- 建立 MyBatis-Plus 基础设施层、dynamic-datasource 严格路由、每数据源独立 HikariCP、公共持久化端口、MySQL 适配器和 Liquibase 根变更集。
- 建立平台 `workflow` 基础模块、Flowable Process Engine 受限 starter、独立
  schema/账号/HikariCP、定义/运行/任务/管理四类稳定端口、BPMN 部署清单和引擎事件桥接；关闭
  REST、IDM、CMMN、DMN、JPA 与生产自动 schema 更新。审批或领域包实现不得先于该基础门禁。
- 建立 `migrationSource`/`migrationTarget` 独立 `SqlSessionFactory`、Mapper 包和事务管理器，验证跨库不使用 XA。
- 接入 OIDC 企业身份，完成 UserAccount 同步、会话撤销、用户停用、工作空间、成员、角色、数据范围和审计骨架。
- 建立契约先行门禁：任何外部接口先修改 OpenAPI、事件或领域包 schema 并使契约测试失败，再实现 Controller 或消费者。
- 建立 CI 质量门禁、容器化本地环境、审计防篡改校验、备份恢复、权限撤销 SLA 和性能基线。

### 阶段 B：平台工作流与配置内核

- 完成平台工作流定义、部署、实例、人工任务、定时器、消息关联、重试、补偿、异常诊断、运行监控
  和流程版本迁移基础能力。
- 完成核心字段目录、领域包三层继承、版本差异、冲突检测和职责分离发布。
- 完成对象、字段、关系、表单、页面、视图、状态机、规则、动作和权限元模型。
- 完成沙箱预览、场景测试、运行时快照和迁移作业。

### 阶段 C：项目执行内核

- 完成模板创建项目、统一生命周期、父子项目和跨工作空间协作。
- 完成任务、检查项、里程碑、依赖、计划基线、可解释进度和多视图。
- 完成交付件版本、审批签核、风险问题变更和归档门禁。
- 审批、项目交付和标准实施领域包复用平台工作流能力编排路由、等待、超时和补偿；各领域聚合负责
  业务决定与状态变化。
- 完成权限过滤的跨项目/跨对象基础搜索、站内通知、失败重提和结果打开时权限复核。
- 每个业务增量同步交付公共 DDL、仓储端口、Mapper/适配器和 MySQL 契约测试。

### 阶段 D：标准实施领域包

- 配置工前准备、施工计划、实施方案、部署、验收和归档子阶段。
- 补充标准对象、必需交付件、审批、完成规则、默认视图和角色权限。
- 使用真实或等价试点项目完成端到端验收，验证配置不修改平台核心。

### 阶段 E：上线硬化

- 完成三副本部署、故障转移、限流、降级、事件重试、备份和季度恢复演练。
- 在 MySQL 8.4 上完成 1000 并发与百万级数据性能测试、权限穿透测试、审计覆盖检查和数据库恢复演练。
- 完成至少两次 MySQL 生产等价全量迁移和彩排切换，验证增量追平、核对、冻结、启用、回退点和旧系统只读保留。
- 完成历史 MySQL 到 PDP MySQL 的生产等价迁移与上线切换演练，验证映射、增量追平、业务核对和单写主权。
- 使用不少于 20 项领域需求验证管理员配置完成率达到 80%，使用不少于 20 名试点成员验证页面引导独立完成率达到 90%。
- 按 FR-165 生成可审计的月度可用性计算样例，并验证所有排除项具有责任方和证据。
- 上线前冻结 SC-020 的人工汇总耗时、重复录入不一致数量、数据来源、统计口径、责任人和三个月复测计划。
- 重新执行威胁模型和 `/speckit-analyze`，关闭 CRITICAL/HIGH 后再执行最终 DoD 签字。
- 建立发布、回滚、MySQL 历史迁移、领域包变更和运营值守手册。

### 阶段 F：上线后价值验证

- 上线满三个月后按冻结口径复测项目状态人工汇总耗时和重复录入不一致问题，验证分别降低至少 60% 和 80%。
- 未达到 SC-020 时记录差距、样本、原因、责任人、整改期限和复测日期，不得通过改变基线或统计口径宣称达标。

## 关键设计规则

- 所有写命令必须携带工作空间上下文；跨空间访问必须命中有效协作授权。
- 高风险写操作使用幂等键；更新使用版本号或 `ETag/If-Match` 防止静默覆盖。
- 核心状态与合法性校验在同一数据库事务完成；通知、搜索投影和外部协同通过事务事件日志异步执行。
- 查询以权限过滤后的读模型为准；搜索索引、缓存和报表不得扩大数据库授权结果。
- 核心字段使用规范化列；扩展字段使用版本化逻辑 JSON 与受控索引投影，P1 映射为 MySQL `JSON`；新业务对象使用通用实例和关系模型。
- UUIDv7 由应用生成；金额和权重使用明确精度定点数；时间统一为 UTC；业务键在应用层规范化，避免依赖数据库默认排序规则和大小写语义。
- 事务隔离显式配置并以乐观锁 `revision` 保证并发语义；不得依赖不同数据库的默认隔离级别或隐式锁行为。
- 核心列表使用签名 keyset cursor；排序必须包含 UUIDv7 唯一兜底键，MyBatis-Plus `Page/IPage` 不得作为外部分页契约。
- 自定义更新必须在 SQL 中校验并递增 `revision`；影响行数为 0 时按权限语义返回 404 或 409。
- UUID、JSON、Instant、稳定枚举和值对象使用统一 TypeHandler；关闭 MyBatis 二级缓存和懒加载，设置 `localCacheScope=STATEMENT`。
- 唯一性、权限、流程条件和游标排序投影必须同事务更新；全文搜索和报表投影可异步，但必须提供数据截止时间。
- 搜索词项由平台统一分析器生成，数据库全文检索只作为候选加速，不能直接决定权限、最终结果集合或稳定排序。
- 事件发布采用 Spring Modulith JDBC 仓储，schema 由 Liquibase 管理，生产关闭自动建表。
- Flowable 只能通过 `workflow` 端口接入；流程变量仅保存编排所需的稳定标识和非敏感快照，不保存
  权威业务对象、完整附件、凭据或最终权限结论。
- Flowable 与业务事务不使用 XA；业务提交、outbox、流程启动/推进和回写均使用幂等键、关联标识和
  可补偿事件桥接，任何重试不得生成重复审批动作或业务结果。
- 所有 Mapper 方法必须显式表达工作空间和数据权限边界；MyBatis-Plus 租户插件不得替代项目、对象、字段和跨工作空间授权校验。
- 外部接口先更新 `contracts/openapi.yaml`、`contracts/events.md` 或 `contracts/domain-package.schema.json`，契约测试确认差异后才能实现 Controller、消费者或领域包运行时。
- 契约任务在每个故事内必须位于领域模型、服务、控制器、消费者和界面任务之前；契约目录维护 P1 故事、接口、消费者、版本、兼容和测试的完整覆盖矩阵。
- 权限撤销后的新请求立即使用最新授权；本地缓存 5 秒内失效，搜索/报表投影 30 秒内移除，实时会话 30 秒内刷新或断开，活动会话和刷新凭据 1 分钟内撤销。
- 禁止在 P1 同时引入 Hibernate/JPA 作为第二套业务持久化框架，避免双持久化上下文、事务边界和实体状态语义分裂。
- 附件访问使用短时签名地址，下载前再次校验对象与字段权限。
- 删除默认采用归档/处置流程；交付件、审批、签核、配置和审计在保留期内不可物理删除。
- 迁移数据必须保留 `source_system`、`source_table`、`source_key`、`migration_run_id` 和 `mapping_version`；在线接口不得向普通用户暴露内部迁移控制字段。
- 风险、问题和变更重开、取消、关闭必须保存原因、操作者和前后状态；批量导入、导出、归档和统计统一使用可暂停、可恢复、可取消的后台作业。

## 项目结构

### 本特性文档

```text
specs/002-pdp-product/
├── spec.md
├── plan.md
├── research.md
├── technology-comparison.md
├── persistence-design.md
├── mysql-migration.md
├── data-model.md
├── terminology.md
├── quickstart.md
├── backlog-p2-p3.md
├── contracts/
│   ├── README.md
│   ├── openapi.yaml
│   ├── domain-package.schema.json
│   ├── migration-report.schema.json
│   ├── coverage.md
│   └── events.md
├── evidence/
└── tasks.md                 # 当前 P1 可执行任务基线
```

### 预期源代码结构

```text
apps/
├── api/
│   ├── pom.xml
│   ├── src/main/java/com/pdp/api/
│   └── src/main/resources/
└── web/
    ├── package.json
    └── src/
        ├── api/
        ├── components/
        ├── router/
        ├── stores/
        ├── styles/
        └── views/

modules/
├── shared-kernel/
├── identity/
├── workspace/
├── domainconfig/
├── template/
├── project/
├── planning/
├── deliverable/
├── workflow/
├── approval/
├── governance/
├── experience/
├── standarddelivery/
├── integration/
├── operations/
├── datamigration/
│   └── src/main/java/com/pdp/datamigration/config/
│       ├── LegacySourceMybatisConfig.java
│       ├── PdpTargetMybatisConfig.java
│       └── MigrationTransactionConfig.java
├── public-persistence/
│   ├── src/main/java/com/pdp/persistence/
│   └── src/main/resources/
│       ├── db/changelog/
│       │   ├── common/
│       │   └── mysql/
│       └── mapper/
└── persistence-mysql/

tests/
├── package.json
├── playwright.config.ts
├── .spectral.yaml
├── backend/
├── contracts/
├── e2e/
├── performance/
└── fixtures/

infra/
├── compose/
├── k8s/
└── observability/

docs/
├── adr/
├── runbooks/
├── security/
└── service-levels/

pom.xml
package.json
pnpm-workspace.yaml
```

**结构决策**：后端先保持单一可部署应用，但按业务能力建立强模块边界；后台任务以同一制品的独立运行配置部署。业务模块在自身包内定义领域模型和持久化端口，MyBatis-Plus、Mapper XML、结果映射和 MySQL 实现集中在 `persistence`；每个业务故事通过独立 DDL 与 Mapper 文件落地，禁止把框架类型或数据库专有分支散落在领域模块。Vue 前端按业务特性组织，页面组合进入 `widgets`，可复用业务模型进入 `entities`，跨特性组件、Composable 和元数据渲染运行时只能进入 `shared`。根 pnpm workspace 统一管理前端、契约、Playwright 和辅助测试工具。Pinia Store 只保存跨页面客户端状态，不缓存可由 TanStack Vue Query 管理的服务器事实。

## 复杂度与例外跟踪

无宪章例外。模块化单体、每部署单一写入主库、P1 单一 MySQL 适配器、数据库无关端口和标准 HTTP/事件契约是满足当前范围的最小可行复杂度；不实施第二种数据库、运行时跨数据库分片、无迁移热切换或双主写入。消息代理、独立搜索集群和微服务拆分均延后到容量数据或组织边界证明必要时。模块化单体、MySQL 持久化边界、动态数据源和迁移架构在实现前分别形成 ADR；后续若申请例外，必须补充违反条款、替代方案、补偿控制、责任人、批准人、失效日期、复审日期和退出条件。
